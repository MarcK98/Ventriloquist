# Copy to terraform.tfvars to override any of these (optional — all have
# sensible defaults below). This example file holds no secrets; the relay's
# actual secrets are generated by Terraform itself and stored in SSM
# Parameter Store (see relay.tf), never in a .tfvars file.

# relay_instance_type = "t3.micro"
# relay_port          = 8820
# vps_instance_type   = "t3.micro"
