{
  "name": "spawn-monorepo",
  "version": "0.1.0",
  "description": "Spawn (spawnmy.ai): local interface to Claude Code team-lead/agents. Monorepo: @spawn/core (orchestration + the legacy Discord bridge) and @spawn/desktop (Electron app).",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node src/index.js",
    "dev": "node packages/core/scripts/dev.js",
    "dev:plain": "node --watch src/index.js",
    "dashboard": "node packages/core/src/dashboard.js",
    "desktop": "npm run dev -w @spawn/desktop"
  },
  "engines": {
    "node": ">=20"
  }
}
